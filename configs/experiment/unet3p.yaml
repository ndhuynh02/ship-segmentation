# @package _global_

# to execute this experiment run:
# python train.py experiment=unet3p

defaults:
  - override /data: airbus.yaml
  - override /model: unet3p.yaml
  - override /callbacks: airbus.yaml
  - override /trainer: gpu.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["ship-segmentation", "unet3p", "train"]

seed: 12345

# need to override transform_train and transform_val by hand in the config files

callbacks:
  wandb_callback:
    _target_: src.utils.callback.wandb_unet3p.WandbCallback
    img_size: 768

logger:
  wandb:
    tags: ${tags}
    group: "ship-segmentation"

ckpt_path: null
